import React from "react";
import ReactDOM from "react-dom/client";
import PullerApp from "./PullerApp";
import "./puller-styles.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PullerApp />
  </React.StrictMode>
);

